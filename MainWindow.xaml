<Window x:Class="NFL_Quiz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NFL_Quiz"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" x:Name="GameBtn" Content="Game" Click="GameButton_Click"  Margin="10"/>
        <Button Grid.Row="1" Grid.Column="0" x:Name="SettingsBtn" Content="Settings" Click="SettingsButton_Click" Margin="10"/>
        <Frame Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
