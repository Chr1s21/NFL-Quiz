<Page x:Class="NFL_Quiz.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NFL_Quiz"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GamePage">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="120" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Text="NFL Quiz" FontSize="56" HorizontalAlignment="Center" />
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" VerticalAlignment="Top">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" x:Name="txtInput" Padding="15"/>
            <ListBox ItemsSource="{Binding Suggestions}" 
                     Visibility="{Binding Suggestions.Count, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>
        <Button IsDefault="True" Grid.Row="1" Grid.Column="3" Content="Submit" Command="{Binding SubmitCommand}" Height="40" VerticalAlignment="Top" Margin="5"/>
    <ItemsControl ItemsSource="{Binding Guesses}" Grid.Row="2" Grid.RowSpan="7">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Player.Name}" Background="{Binding nameBrush}" Grid.Column="0"/>
                    <TextBlock Text="{Binding TrikotDisplay}" Background="{Binding trikotBrush}" Grid.Column="1"/>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>     
    </Grid>
</Page>
